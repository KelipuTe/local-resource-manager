
const config = {
    dbTextDefaultValue: '0',
    dbTextDefaultValueNowTime: 'CURRENT_TIMESTAMP',
}

const dbTextDefaultValue = '0';
const dbTextDefaultValueNowTime = 'CURRENT_TIMESTAMP';

module.exports = {
    config: config,
    dbTextDefaultValue,
    dbTextDefaultValueNowTime
}